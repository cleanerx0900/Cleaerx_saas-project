// Platform brand default colors — used as fallback values when a company has
// not yet configured its own theme, and for the platform's own auth/admin UI
// (login, change-password, admin panel) which always uses the platform brand.
export const NAVY   = '#0A1F44'  // primary
export const GOLD   = '#D4AF37'  // secondary
export const ACCENT = '#F5C518'  // accent
